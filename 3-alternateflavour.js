module.exports.items = ['items1', 'item2'];
const person = {
	name: 'bruno',
};

// We can use any kind of export it is just an alternate of export
//Since export is object in module we are just adding an element inside object
module.exports.singlePerson = person;
